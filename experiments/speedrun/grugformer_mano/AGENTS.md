# Agent Instructions: grugformer_mano

- After any run attempt (success, partial, or failure), update `RUNS.md` in this directory.
- Add a new run entry using the fixed template in `RUNS.md`; do not overwrite prior entries.
- Always record:
  - W&B run link (or reason missing),
  - exact key env/config deltas,
  - core metrics (including `eval/paloma/c4_en/bpb` when available),
  - tradeoffs made,
  - concrete next steps to reach a full submission.
- Keep entries factual and concise; avoid speculative claims.
